head
    title= title
    script(src='https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js')
    script(src='track.js')
    style
        include ../static/coffee.css
body
    h1 Coffee Tracker
    div.label.cent(style = "text-align:center;color: #fff") Login
      div.username
        input.center(type="text" id = 'name' placeholder="username")
      div.password.cent
        input.center(type="password" id = 'pass' placeholder="password")
     
      button.button.center(onclick="movep()") Login
      p
        | Don't have an account?
      button.button.center(onclick="move()") Sign Up
script.
  function move() {
    window.location.href = '/signup'
  }
  function movep() {
    window.location.href = '/home'
  }
